apiVersion: 1

datasources:
  - name: PostgreSQL
    uid: PostgreSQL
    type: postgres
    url: localhost:5432
    database: agent_telemetry
    user: telemetry
    secureJsonData:
      password: ${POSTGRES_PASSWORD}
    jsonData:
      database: agent_telemetry
      sslmode: disable
      maxOpenConns: 10
      maxIdleConns: 5
      connMaxLifetime: 14400
    isDefault: true
    editable: false
